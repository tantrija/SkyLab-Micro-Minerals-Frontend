/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'api-skylab.tantrija.com',
        ],
    },
};

export default nextConfig;

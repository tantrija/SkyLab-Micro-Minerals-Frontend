/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'api-royalmicron.tantrija.com',
        ],
    },
};

export default nextConfig;
